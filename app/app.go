package app

// Commit git commit id (set in build time)
var Commit string

// Version git last tag (set in build time)
var Version string

// BuildDate binary build date (set in build time)
var BuildDate string
